<Window x:Class="RemoteCapture.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteCapture.Viewer"
        mc:Ignorable="d"
        Title="Remote Desktop Viewer" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="ServerAddressTextBox" Width="200" Text="localhost:5000" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button x:Name="ConnectButton" Content="Connect" Width="100" Click="ConnectButton_Click" Margin="0,0,10,0"/>
            <Button x:Name="DisconnectButton" Content="Disconnect" Width="100" Click="DisconnectButton_Click" IsEnabled="False"/>
            <TextBlock x:Name="StatusTextBlock" Text="Disconnected" VerticalAlignment="Center" Margin="20,0,0,0" FontWeight="Bold"/>
        </StackPanel>

        <Border Grid.Row="1" Background="Black" Margin="10">
            <Viewbox Stretch="Uniform">
                <Image x:Name="ScreenImage" Stretch="None" Focusable="True"/>
            </Viewbox>
        </Border>
    </Grid>
</Window>
